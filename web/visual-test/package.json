{
  "name": "curio-ui-visual-test",
  "private": true,
  "type": "module",
  "description": "Dev-only Playwright visual regression tests for Curio admin UI",
  "scripts": {
    "serve": "node serve-static.mjs",
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
